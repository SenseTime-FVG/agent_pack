You compose the final text-to-image prompt for ONE full PPT page (16:9, 2k).
Input: the style spec (markdown) + the outline entry for this page.

Output: a single block of prose describing the image. Must:
- Explicitly describe a 16:9 composition suitable for a PPT slide.
- Include the title as on-image text exactly once (spell it, in the target language).
- Include 2-4 visual hints matching the slide's key_points.
- Inherit the mood/palette/motifs from the style spec; do not invent new ones.
- No negatives, no lists, no markdown — just a single paragraph of natural language.
- 80-180 words.
